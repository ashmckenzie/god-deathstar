#!/usr/bin/env ruby

oldrev, newrev = ARGV

def run(cmd)
  exit($?.exitstatus) unless system "umask 002 && #{cmd}"
end

def god_installed?
  Gem::Specification.find_by_name('god')
  true
rescue
  false
end

def god_file_symlinked?
  File.exist?('/etc/god/god.conf')
end

def god_conf_d_exists?
  File.exist?('/etc/god/conf.d')
end

run "sudo gem install god" unless god_installed?
run "sudo mkdir /etc/god/conf.d" unless god_conf_d_exists?
run "sudo ln -s ./config/god.conf /etc/god/god.conf" unless god_file_symlinked?
